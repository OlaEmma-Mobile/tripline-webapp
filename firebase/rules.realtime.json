{
  "rules": {
    "realtime": {
      "trips": {
        "$tripId": {
          ".read": true,
          ".write": false
        }
      },
      "rides": {
        "$rideId": {
          ".read": true,
          ".write": false
        }
      },
      "notifications": {
        "$userId": {
          ".read": "auth != null && auth.uid == $userId",
          ".write": "auth != null && auth.uid == $userId"
        }
      }
    }
  }
}
